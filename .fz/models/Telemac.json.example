{
    "id": "Telemac",
    "varprefix": "$", 
    "formulaprefix": "@", 
    "delim": "()", 
    "commentline": "/",
    "output": {
        "_comment1": "Define output parsers based on your simulation results.",
        "_comment2": "Examples below show different ways to extract output data.",
        
        "_example_simple": "grep 'Maximum depth:' output.txt | awk '{print $3}'",
        
        "_example_csv_S": "python -c 'import pandas;import glob;import json;print(json.dumps({f.split(\"_S.csv\")[0]:pandas.read_csv(f).to_dict() for f in glob.glob(\"*_S.csv\")}))'",
        "_example_csv_H": "python -c 'import pandas;import glob;import json;print(json.dumps({f.split(\"_H.csv\")[0]:pandas.read_csv(f).to_dict() for f in glob.glob(\"*_H.csv\")}))'",
        
        "_example_multiple": "cat results.txt | grep 'velocity' | awk '{print $2}'"
    }
}
